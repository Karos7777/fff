# 🚀 Руководство по реальному тестированию платежей

## 📋 Что нужно для тестирования

### 1. **Настройка переменных окружения**

В файле `.env` должны быть указаны:

```env
# Telegram Bot
BOT_TOKEN=your_real_bot_token

# TON Blockchain  
TON_WALLET_ADDRESS=your_ton_wallet_address_for_receiving_payments
TON_API_KEY=your_tonapi_key_from_tonapi_io

# JWT для безопасности
JWT_SECRET=your_super_secret_jwt_key

# Порт
PORT=10000
```

### 2. **Получение необходимых токенов**

#### **BOT_TOKEN (Telegram Bot)**
1. Найдите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте полученный токен

#### **TON_WALLET_ADDRESS (ваш кошелек)**
1. Откройте ваш TON кошелек (Tonkeeper, TON Wallet и т.д.)
2. Скопируйте адрес кошелька (формат: `EQC...`)
3. **Важно:** Это должен быть кошелек, на который вы хотите получать платежи

#### **TON_API_KEY (TonAPI)**
1. Зайдите на [tonapi.io](https://tonapi.io)
2. Зарегистрируйтесь
3. Создайте API ключ в личном кабинете
4. Скопируйте ключ

## 🧪 Тестирование

### **Запуск тестирования**

1. **Запустите сервер:**
   ```bash
   npm start
   ```

2. **Откройте страницу тестирования:**
   ```
   http://localhost:10000/real-test.html
   ```

### **Тестовые товары созданы:**

- **ID 14:** "Тест TON - Консультация 15 мин" - **0.01 TON**
- **ID 15:** "Тест Stars - Мини-консультация" - **1 звезда**

## ⭐ Тестирование Telegram Stars

### **Шаги:**
1. Нажмите "Купить за 1 звезду"
2. Выберите "Telegram Stars"
3. Нажмите "Оплатить Stars"
4. **Система отправит инвойс в ваш Telegram**
5. Подтвердите оплату в Telegram
6. **Автоматически:** Статус заказа обновится на "Оплачен"

### **Что происходит:**
- Создается инвойс через Telegram Bot API
- Telegram обрабатывает оплату
- Webhook уведомляет ваш сервер об успешной оплате
- Заказ автоматически помечается как оплаченный
- Пользователь получает уведомление

## 💎 Тестирование TON

### **Шаги:**
1. Нажмите "Купить за 0.01 TON"
2. Выберите "Криптовалюта" → "TON"
3. **Скопируйте адрес кошелька**
4. **Скопируйте memo (комментарий)**
5. Откройте ваш TON кошелек
6. Отправьте **0.01 TON** на указанный адрес
7. **Обязательно укажите memo в комментарии!**
8. Подтвердите транзакцию
9. Ждите 1-2 минуты

### **Что происходит:**
- Система каждые 2 минуты проверяет новые транзакции через TonAPI
- Ищет транзакции с правильным memo и суммой
- При обнаружении платежа обновляет статус заказа
- Отправляет уведомление пользователю

## 🔍 Отладка

### **Если Stars не работает:**
1. Проверьте `BOT_TOKEN` в `.env`
2. Убедитесь, что бот создан через @BotFather
3. Проверьте логи сервера на ошибки

### **Если TON не работает:**
1. Проверьте `TON_WALLET_ADDRESS` - правильный ли адрес?
2. Проверьте `TON_API_KEY` - работает ли ключ?
3. Убедитесь, что указали правильный memo в транзакции
4. Проверьте сумму - должно быть точно 0.01 TON

### **Проверка логов:**
```bash
# В консоли сервера смотрите сообщения:
✅ Сервис платежей инициализирован
✅ Автоматические задачи платежей настроены
```

## 📊 Мониторинг

### **Проверка статуса заказов:**
```sql
-- Подключитесь к базе данных и выполните:
SELECT * FROM orders ORDER BY created_at DESC LIMIT 10;
SELECT * FROM invoices ORDER BY created_at DESC LIMIT 10;
```

### **Проверка транзакций:**
```sql
SELECT * FROM transactions ORDER BY created_at DESC LIMIT 10;
```

## 🎉 Ожидаемый результат

После успешной оплаты:
1. ✅ Статус заказа изменится на "paid"
2. 📱 Пользователь получит уведомление в Telegram
3. 📊 Транзакция запишется в базу данных
4. 🎯 Система будет готова к продакшену

## 🚀 Деплой на Railway

После успешного тестирования:

1. **Добавьте все файлы в Git:**
   ```bash
   git add .
   git commit -m "feat: complete payment system integration"
   git push
   ```

2. **Настройте переменные окружения на Railway:**
   - `BOT_TOKEN`
   - `TON_WALLET_ADDRESS` 
   - `TON_API_KEY`
   - `JWT_SECRET`
   - `PORT=10000`

3. **Railway автоматически задеплоит обновления**

## ⚠️ Важные моменты

1. **Безопасность:** Никогда не коммитьте `.env` файл в Git
2. **Тестирование:** Используйте минимальные суммы для тестов
3. **Мониторинг:** Следите за логами при первых реальных платежах
4. **Backup:** Регулярно делайте бэкап базы данных

**Удачного тестирования! 🎯**
