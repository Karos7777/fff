# 🎯 СЛЕДУЮЩИЕ ШАГИ — ФИНАЛЬНОЕ ТЕСТИРОВАНИЕ

## ✅ Что исправлено

1. ✅ PostgreSQL синтаксис в `payment-service.js`
2. ✅ Amount теперь сохраняется правильно
3. ✅ Ультра-подробное логирование
4. ✅ Polling работает каждые 8 секунд

---

## 🚀 ЧТО ДЕЛАТЬ СЕЙЧАС

### 1. Очисти старые заказы (опционально)

Подключись к БД и выполни:

```sql
DELETE FROM invoices 
WHERE (amount = 0 OR amount IS NULL) 
  AND currency = 'TON' 
  AND status = 'pending';
```

Или через Railway CLI:
```bash
railway run psql $DATABASE_URL -c "DELETE FROM invoices WHERE amount = 0 AND currency = 'TON' AND status = 'pending';"
```

### 2. Создай НОВЫЙ заказ

1. Открой Mini App
2. Выбери товар
3. "Купить" → "Криптовалюта" → "TON"
4. Получи QR-код

### 3. Проверь логи создания инвойса

```bash
railway logs --follow | grep "TON INVOICE"
```

**Ожидается:**
```
[TON INVOICE] Параметры: { orderId: 114, userId: 1, amount: 0.001, ... }
[TON INVOICE] SQL параметры: [114, 1, 0.001, 'TON', ...]  ← ✅ amount = 0.001
[TON INVOICE] УСПЕШНО: { id: 150, amount: 0.001 }  ← ✅ НЕ 0!
```

### 4. Оплати 0.001 TON

Отсканируй QR-код в TON кошельке и отправь 0.001 TON.

### 5. Смотри логи polling (каждые 8 сек)

```bash
railway logs --follow | grep "TON POLLING"
```

**Ожидается:**
```
[TON POLLING] Проверяем 1 инвойсов: order_114
[TON POLLING] Найдено 10 транзакций:
   → 0.001000 TON | от EQDyRtMn... → EQCm27jo... | hash: abc123...
[TON POLLING] Ищем для заказа #114 (order_114):
   Ожидается: 0.001 TON (1000000 nanoTON)  ← ✅ ПРАВИЛЬНО!
   Минимум: 0.000900 TON (900000 nanoTON)

✅ [TON POLLING] ОПЛАТА ЗАСЧИТАНА! Заказ #114
   Получено: 0.001000 TON (1000000 nanoTON)
   Ожидалось: 0.001 TON (1000000 nanoTON)
   Hash: abc123def456...
   Разница: 0.00%
```

### 6. Проверь статус заказа

В Mini App → "Мои заказы" → Заказ #114 должен быть `paid` ✅

### 7. Скачай файл

Кнопка "📥 Скачать файл" должна появиться автоматически.

---

## 📊 Проверка через БД

### Проверь, что новый инвойс имеет правильную сумму:

```sql
SELECT id, order_id, amount, currency, status, created_at 
FROM invoices 
WHERE currency = 'TON'
ORDER BY id DESC 
LIMIT 5;
```

**Ожидается:**
```
id  | order_id | amount | currency | status  | created_at
150 | 114      | 0.001  | TON      | pending | 2025-11-06 22:00:00  ← ✅ amount = 0.001
149 | 113      | 0      | TON      | pending | 2025-11-06 21:50:00  ← ❌ старый
```

---

## 🎉 КРИТЕРИИ УСПЕХА

### ✅ Инвойс создан правильно:
```
[TON INVOICE] SQL параметры: [..., 0.001, ...]
```

### ✅ Polling видит правильную сумму:
```
[TON POLLING] Ожидается: 0.001 TON (1000000 nanoTON)
```

### ✅ Транзакция найдена:
```
✅ [TON POLLING] ОПЛАТА ЗАСЧИТАНА! Заказ #114
```

### ✅ Статус обновлён:
```sql
SELECT status FROM orders WHERE id = 114;
-- Результат: paid
```

### ✅ Файл доступен для скачивания:
```
GET /api/orders/114/download?token=...
-- Результат: 200 OK, файл скачивается
```

---

## 🐛 Если что-то не работает

### Проблема 1: Amount всё ещё 0
```
[TON INVOICE] SQL параметры: [..., 0, ...]
```

**Решение:** Проверь, что изменения в `payment-service.js` задеплоены:
```bash
git status
git log -1
```

### Проблема 2: Транзакция не находится
```
[TON POLLING] ❌ Транзакция не найдена
```

**Решение:** 
1. Проверь, что оплатил на правильный адрес
2. Подожди 1-2 минуты (задержка блокчейна)
3. Проверь транзакцию на https://tonviewer.com

### Проблема 3: Polling не запускается
```
⚠️  TON_WALLET_ADDRESS не задан - TON polling отключён
```

**Решение:** Проверь переменную окружения:
```bash
railway variables
```

---

## 📝 Чеклист

- [ ] Деплой изменений (`git push`)
- [ ] Очистка старых заказов (SQL)
- [ ] Создание нового заказа
- [ ] Проверка логов создания инвойса
- [ ] Оплата 0.001 TON
- [ ] Проверка логов polling
- [ ] Проверка статуса заказа
- [ ] Скачивание файла

---

## 🎯 ФИНАЛ

**Если все шаги выполнены и оплата засчиталась:**

🎉 **ПОЗДРАВЛЯЮ! TON ПЛАТЕЖИ РАБОТАЮТ!** 🎉

**Полный цикл:**
1. Создание заказа → ✅
2. Генерация QR-кода → ✅
3. Оплата в TON кошельке → ✅
4. Автоматическое засчитывание (polling) → ✅
5. Обновление статуса → ✅
6. Скачивание файла → ✅

**ГОТОВО К ПРОДАКШЕНУ!** 🚀💎
