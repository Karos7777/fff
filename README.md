# Telegram Magazine Bot

Telegram бот-магазин с поддержкой криптовалютных платежей и Telegram Stars.

## Структура проекта

```
├── server.js                 # Основной файл сервера
├── package.json             # Зависимости проекта
├── .env                     # Переменные окружения (не в git)
├── db/
│   └── index.js            # Адаптер базы данных
├── db-postgres.js          # PostgreSQL адаптер
├── middleware/
│   └── auth.js             # Middleware авторизации
├── services/
│   ├── database.js         # Сервис инициализации БД
│   ├── payment.js          # Сервис платежей
│   └── tonPolling.js       # Мониторинг TON платежей
├── routes/
│   ├── admin/              # Админские роуты
│   ├── auth/               # Авторизация
│   ├── payments/           # Платежи
│   ├── products/           # Товары
│   ├── reviews/            # Отзывы
│   ├── telegram/           # Telegram вебхуки
│   ├── users/              # Пользователи
│   └── orders/             # Заказы
└── public/                 # Статические файлы
    ├── index.html          # Главная страница
    ├── admin.html          # Админ панель
    ├── app.js              # Основной JS
    ├── payment.js          # Логика платежей
    └── uploads/            # Загруженные файлы
```

## Переменные окружения

Создайте файл `.env` с следующими переменными:

```env
# База данных PostgreSQL (Railway)
DATABASE_URL=postgresql://username:password@host:port/database

# Telegram Bot
BOT_TOKEN=your_telegram_bot_token
ADMIN_TELEGRAM_IDS=853232715,another_admin_id

# JWT Secret (минимум 32 символа)
JWT_SECRET=your_very_long_and_secure_jwt_secret_key_here

# TON Wallet (опционально)
TON_WALLET_ADDRESS=UQBvW8Z5huBkMJYdnfAEM5XONfNEX5iVhQDMhFWlOHVEz8_a

# Порт (автоматически устанавливается Railway)
PORT=10000
```

## Установка и запуск

1. Установите зависимости:
```bash
npm install
```

2. Настройте переменные окружения в `.env`

3. Запустите сервер:
```bash
npm start
```

## Основные функции

### Для пользователей:
- 🛍️ Просмотр каталога товаров
- 💳 Оплата через Telegram Stars
- 🪙 Оплата криптовалютами (TON, USDT)
- ⭐ Система отзывов и рейтингов
- 📦 Отслеживание заказов

### Для администраторов:
- ➕ Добавление/редактирование товаров
- 📊 Управление заказами
- 👥 Управление пользователями
- 💰 Отслеживание платежей

## API Endpoints

### Авторизация
- `POST /api/auth/telegram` - Авторизация через Telegram
- `GET /api/user/profile` - Профиль пользователя
- `GET /api/user/role` - Роль пользователя

### Товары
- `GET /api/products` - Список товаров
- `GET /api/products/:id` - Товар по ID
- `POST /api/admin/products` - Создание товара (админ)
- `PUT /api/admin/products/:id` - Обновление товара (админ)
- `DELETE /api/admin/products/:id` - Удаление товара (админ)

### Заказы
- `GET /api/orders` - Заказы пользователя
- `POST /api/orders` - Создание заказа
- `POST /api/orders/:id/expire` - Отмена заказа

### Платежи
- `POST /api/payments/stars/create-invoice` - Создание Stars инвойса
- `GET /api/payments/stars/status/:paymentId` - Статус платежа
- `POST /api/telegram/webhook` - Telegram вебхук

### Отзывы
- `POST /api/reviews` - Добавление отзыва
- `GET /api/products/:id/reviews` - Отзывы товара

## Технологии

- **Backend**: Node.js, Express.js
- **Database**: PostgreSQL (Railway)
- **Authentication**: JWT
- **Payments**: Telegram Stars, TON, USDT
- **Frontend**: Vanilla JS, HTML, CSS
- **Deployment**: Railway

## Безопасность

- JWT токены для авторизации
- Проверка прав доступа для админских функций
- Валидация всех входящих данных
- Защита от SQL инъекций через параметризованные запросы

## Мониторинг

- Автоматическая проверка криптоплатежей каждые 30 секунд
- Очистка просроченных инвойсов каждые 10 минут
- Автоудаление истёкших заказов каждые 5 минут
- Health check endpoint: `/healthz`
