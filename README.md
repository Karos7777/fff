# Telegram Магазин Бот

Telegram мини-приложение магазин с админкой для продажи услуг.

## 🚀 Последние исправления

### ✅ Исправленные ошибки:
- **Удален дублирующий код**: Убраны неиспользуемые админские роуты `/api/admin2/*`
- **Исправлена безопасность**: Убран хардкод JWT_SECRET, теперь используется переменная окружения
- **Добавлен недостающий endpoint**: `/api/admin/stats` для админ панели
- **Исправлены глобальные переменные**: Добавлены объявления `window.currentUser` и `window.orders`
- **Улучшена функция generateStars**: Более точное отображение рейтинга
- **Добавлены недостающие переводы**: `langRu`, `langEn`, ценовые диапазоны
- **Удалены отладочные логи**: Убраны лишние `console.log` из продакшн кода

### 🗑️ Удаленные лишние файлы:
- `ngrok/ngrok.exe` (29MB) - исполняемый файл не должен быть в репозитории
- `public/uploads/1750767317333-1 .png` - тестовое изображение с пробелом в имени
- Удалены неиспользуемые webpack зависимости из package.json

### 📁 Добавленные файлы:
- `.gitignore` - для исключения ненужных файлов из репозитория
- `public/uploads/.gitkeep` - для сохранения структуры папки uploads

## 🛠️ Установка и запуск

1. **Клонируйте репозиторий:**
```bash
git clone <repository-url>
cd tg_magazin_bot
```

2. **Установите зависимости:**
```bash
npm install
```

3. **Настройте переменные окружения:**
Создайте файл `.env` в корне проекта:
```env
JWT_SECRET=your-super-secret-jwt-key-here
PORT=3000
```

4. **Запустите сервер:**
```bash
# Для разработки
npm run dev

# Для продакшена
npm start
```

## 🔧 Основные функции

### Для пользователей:
- 🔐 Авторизация через Telegram
- 🛍️ Просмотр каталога товаров
- 🔍 Поиск и фильтрация
- ⭐ Система отзывов
- 📱 Адаптивный дизайн
- 🌐 Поддержка русского и английского языков

### Для администраторов:
- 📊 Дашборд со статистикой
- 📦 Управление товарами (CRUD)
- 📋 Управление заказами
- 👥 Управление пользователями
- 🖼️ Загрузка изображений

## 🗄️ База данных

Проект использует **better-sqlite3** (современная замена sqlite3) с автоматическим созданием таблиц:
- `users` - пользователи и администраторы
- `products` - товары/услуги
- `orders` - заказы
- `reviews` - отзывы

### Преимущества better-sqlite3:
- ✅ Поддержка prebuilt-бинарников для Linux (отлично работает на Render)
- ✅ Синхронный API (проще в использовании)
- ✅ Лучшая производительность
- ✅ Более современная архитектура
## 🔒 Безопасность

- JWT токены для аутентификации
- Middleware для проверки прав администратора
- Валидация входных данных
- Защита от SQL инъекций через параметризованные запросы

## 📱 Telegram интеграция

Проект интегрирован с Telegram Web App API:
- Автоматическая авторизация через Telegram
- Адаптация под Telegram Mini App
- Поддержка Telegram-специфичных функций

## 🌐 API Endpoints

### Публичные:
- `GET /` - главная страница
- `GET /admin` - админ панель
- `GET /api/products` - список товаров
- `GET /api/products/:id` - товар по ID
- `GET /api/products/:id/reviews` - отзывы товара

### Аутентифицированные:
- `POST /api/auth` - авторизация
- `POST /api/orders` - создание заказа
- `GET /api/orders` - заказы пользователя
- `POST /api/reviews` - добавление отзыва

### Админские:
- `GET /api/admin/stats` - статистика
- `GET /api/admin/products` - все товары
- `POST /api/admin/products` - создание товара
- `PUT /api/admin/products/:id` - обновление товара
- `DELETE /api/admin/products/:id` - удаление товара
- `GET /api/admin/orders` - все заказы
- `DELETE /api/admin/orders/:id` - удаление заказа
- `GET /api/admin/users` - все пользователи

## 🎨 Технологии

- **Backend**: Node.js, Express.js
- **Database**: better-sqlite3 (современная замена SQLite3)
- **Frontend**: Vanilla JavaScript, HTML5, CSS3
- **Authentication**: JWT, bcryptjs
- **File Upload**: Multer
- **Styling**: CSS Grid, Flexbox, CSS Variables
- **Cloud Ready**: Prebuilt binaries для Linux/Render

## 📝 Лицензия

MIT License

## 🚨 Решение проблем

### Ошибка "EADDRINUSE: address already in use"

Если вы получаете ошибку о том, что порт уже используется:

1. **Автоматическое решение**: Приложение теперь автоматически найдет свободный порт
2. **Альтернативные скрипты**: 
   ```bash
   npm run start:alt    # запуск на порту 3001
   npm run dev:alt      # разработка на порту 3001
   ```
3. **Убить процессы Node.js**: 
   ```bash
   taskkill /f /im node.exe
   ```

### Проверка занятых портов
```bash
# Windows PowerShell
Get-NetTCPConnection -LocalPort 3000 -ErrorAction SilentlyContinue

# Windows CMD
netstat -ano | findstr :3000
```

## 🌐 Деплой на Render

### Переменные окружения
Настройте следующие переменные в Render:

```env
BOT_TOKEN=your_telegram_bot_token_here
JWT_SECRET=your_super_secret_jwt_key_here
PORT=10000
```

### Настройка деплоя
1. **Build Command**: `npm run build` (простая проверка, без webpack)
2. **Start Command**: `npm start`
3. **Node Version**: 18+ (рекомендуется)

### Health Check
Приложение включает endpoint `/healthz` для предотвращения засыпания на бесплатном тарифе.

### Почему нет webpack?
Проект использует простую архитектуру:
- Статические файлы отдаются из папки `public/`
- Нет необходимости в сборке фронтенда
- better-sqlite3 поставляется с prebuilt-бинарниками для Linux

### Telegram Bot Integration
- Добавлен endpoint `/api/notify-order` для отправки уведомлений в Telegram
- Все токены берутся из переменных окружения (безопасно)
- Автоматическая проверка наличия BOT_TOKEN при запуске

## 🔄 Миграция на better-sqlite3

### Проблема с sqlite3 в облаке
Если вы видите ошибку:
```
Error: .../node_sqlite3.node: invalid ELF header
code: 'ERR_DLOPEN_FAILED'
```

Это означает, что нативный модуль sqlite3 был скомпилирован под другую ОС и не совместим с Linux-окружением облачных платформ.

### Решение
✅ **Проект уже переведен на better-sqlite3!**

- Убран устаревший `sqlite3`
- Добавлен современный `better-sqlite3` с prebuilt-бинарниками
- Переписан весь код для синхронного API
- Улучшена обработка ошибок
- Добавлены информативные логи при инициализации БД

### Проблема с webpack на Render
Если вы видите ошибку:
```
webpack: command not found
```

**Причина**: В `package.json` был неправильный build-скрипт с webpack, который не нужен для этого проекта.

**Решение**: ✅ Уже исправлено!
- Удален ненужный webpack из зависимостей
- Добавлен простой build-скрипт без сборки
- Проект использует статические файлы из `public/` без сборки

## 🤝 Поддержка

Если у вас есть вопросы или предложения, создайте issue в репозитории.

---
*Последнее обновление: November 2025 - Migrated to better-sqlite3 for cloud compatibility*