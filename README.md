# Telegram Мини-Приложение Магазин

Полнофункциональное Telegram мини-приложение для продажи услуг с админ-панелью для управления товарами и заказами.

## 🚀 Возможности

### Для покупателей:
- Просмотр каталога услуг
- Фильтрация по категориям
- Поиск по названию и описанию
- Авторизация через Telegram
- Создание заказов
- Просмотр истории заказов

### Для администраторов:
- Полноценная админ-панель
- Управление товарами (добавление, редактирование, удаление)
- Загрузка изображений для товаров
- Управление заказами (изменение статусов)
- Просмотр статистики
- Управление пользователями

## 📋 Требования

- Node.js 14+ 
- npm или yarn

## 🛠️ Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd tg_magazin_bot
```

2. Установите зависимости:
```bash
npm install
```

3. Настройте переменные окружения (опционально):
```bash
# Создайте файл .env
JWT_SECRET=your-secret-key-here
PORT=3000
```

4. Запустите сервер:
```bash
npm start
```

Для разработки используйте:
```bash
npm run dev
```

## 🔧 Настройка

### Настройка админа

По умолчанию создается админ с Telegram ID `123456789`. Чтобы изменить на свой:

1. Откройте файл `server.js`
2. Найдите строку:
```javascript
db.run(`INSERT OR IGNORE INTO users (telegram_id, username, is_admin) VALUES ('123456789', 'admin', 1)`);
```
3. Замените `123456789` на ваш Telegram ID

### Получение Telegram ID

1. Напишите боту @userinfobot в Telegram
2. Он покажет ваш ID

## 📱 Использование

### Основное приложение
- Откройте `http://localhost:3000`
- Авторизуйтесь через Telegram
- Просматривайте и заказывайте услуги

### Админ-панель
- Откройте `http://localhost:3000/admin`
- Используйте аккаунт с правами администратора
- Управляйте товарами и заказами

## 🗂️ Структура проекта

```
tg_magazin_bot/
├── server.js              # Основной сервер
├── package.json           # Зависимости
├── public/                # Статические файлы
│   ├── index.html         # Главная страница
│   ├── admin.html         # Админ-панель
│   ├── styles.css         # Стили для основного приложения
│   ├── admin-styles.css   # Стили для админки
│   ├── app.js             # JavaScript основного приложения
│   ├── admin.js           # JavaScript админки
│   └── uploads/           # Загруженные изображения
├── shop.db                # База данных SQLite
└── README.md              # Документация
```

## 🗄️ База данных

Приложение использует SQLite с тремя основными таблицами:

### users
- `id` - уникальный идентификатор
- `telegram_id` - Telegram ID пользователя
- `username` - имя пользователя
- `is_admin` - права администратора
- `created_at` - дата регистрации

### products
- `id` - уникальный идентификатор
- `name` - название товара
- `description` - описание
- `price` - цена
- `image_url` - путь к изображению
- `category` - категория
- `is_active` - активен ли товар
- `created_at` - дата создания

### orders
- `id` - уникальный идентификатор
- `user_id` - ID пользователя
- `product_id` - ID товара
- `status` - статус заказа
- `created_at` - дата создания

## 🔌 API Endpoints

### Публичные
- `POST /api/auth` - авторизация пользователя
- `GET /api/products` - получение списка товаров
- `GET /api/products/:id` - получение товара по ID

### Для авторизованных пользователей
- `POST /api/orders` - создание заказа
- `GET /api/orders` - получение заказов пользователя

### Для администраторов
- `GET /api/admin/products` - получение всех товаров
- `POST /api/admin/products` - добавление товара
- `PUT /api/admin/products/:id` - обновление товара
- `DELETE /api/admin/products/:id` - удаление товара
- `GET /api/admin/orders` - получение всех заказов
- `PUT /api/admin/orders/:id/status` - изменение статуса заказа
- `GET /api/admin/users` - получение всех пользователей
- `GET /api/admin/stats` - получение статистики

## 🎨 Кастомизация

### Добавление новых категорий

1. Обновите опции в HTML файлах:
```html
<option value="new_category">Новая категория</option>
```

2. Обновите функцию `getCategoryName` в JavaScript файлах:
```javascript
const categories = {
    'development': 'Разработка',
    'consultation': 'Консультации',
    'design': 'Дизайн',
    'new_category': 'Новая категория',
    'other': 'Другое'
};
```

### Изменение стилей

Отредактируйте файлы CSS:
- `public/styles.css` - стили основного приложения
- `public/admin-styles.css` - стили админ-панели

## 🚀 Развертывание

### Локальное развертывание
```bash
npm start
```

### Развертывание на сервере

1. Загрузите файлы на сервер
2. Установите зависимости: `npm install`
3. Настройте переменные окружения
4. Запустите с помощью PM2:
```bash
npm install -g pm2
pm2 start server.js --name "tg-shop"
pm2 startup
pm2 save
```

## 🔒 Безопасность

- Все админские операции защищены JWT токенами
- Проверка прав доступа на уровне middleware
- Валидация входных данных
- Безопасная загрузка файлов

## 🐛 Устранение неполадок

### Проблемы с авторизацией
- Проверьте правильность Telegram ID в настройках админа
- Убедитесь, что JWT_SECRET установлен

### Проблемы с загрузкой изображений
- Проверьте права доступа к папке `public/uploads`
- Убедитесь, что папка существует

### Проблемы с базой данных
- Удалите файл `shop.db` для пересоздания базы данных
- Проверьте права доступа к файлу

## 📞 Поддержка

Если у вас возникли вопросы или проблемы, создайте issue в репозитории.

## 📄 Лицензия

MIT License 