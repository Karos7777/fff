# 🔄 Руководство по обновлению версии приложения

## Обзор системы версионирования

Теперь приложение использует **автоматическую систему версионирования**, которая:
- ✅ Автоматически очищает кеш при обновлении
- ✅ Сохраняет важные данные (токен авторизации, пользователя)
- ✅ Перезагружает страницу для применения изменений
- ✅ Предотвращает проблемы с кешированием

---

## 📝 Как обновить версию

### Шаг 1: Измените версию в коде

Откройте файл `public/app.js` и измените константу `APP_VERSION`:

```javascript
// Было:
const APP_VERSION = '2.0.0';

// Стало (например):
const APP_VERSION = '2.0.1';
```

### Шаг 2: Обновите версию в HTML файлах

Откройте файлы и измените версию в тегах `<script>`:

**`public/index.html`:**
```html
<script src="app.js?v=2.0.1"></script>
<script src="reviews.js?v=2.0.1"></script>
<script src="payment.js?v=2.0.1"></script>
```

**`public/admin.html`:**
```html
<script src="admin.js?v=2.0.1"></script>
```

### Шаг 3: Сохраните изменения и перезапустите сервер

```bash
# Остановите сервер (Ctrl+C)
# Запустите снова
node server.js
```

---

## 🔍 Что происходит при обновлении

Когда пользователь открывает приложение после обновления:

1. **Проверка версии** - сравнивается текущая версия с сохраненной
2. **Очистка кеша** - удаляются старые данные из localStorage и cache storage
3. **Сохранение важных данных** - токен и данные пользователя сохраняются
4. **Перезагрузка** - страница автоматически перезагружается
5. **Применение изменений** - новая версия загружается без кеша

### Логи в консоли:

```
🔄 [VERSION] Текущая версия: 2.0.1
🔄 [VERSION] Сохраненная версия: 2.0.0
⚠️ [VERSION] Обнаружено обновление! Очистка кеша...
✅ [VERSION] Кеш браузера очищен
✅ [VERSION] Обновление завершено. Версия: 2.0.1
🔄 [VERSION] Перезагрузка страницы...
```

---

## 🗑️ Проверка и очистка базы данных

### Проверить все товары в БД:

```bash
node check-products.js
```

Вы увидите:
- Список всех товаров с ID, названием, ценой
- Количество активных/неактивных товаров
- Список тестовых товаров (если есть)

### Удалить тестовые товары:

```bash
node delete-test-products.js
```

Скрипт автоматически найдет и удалит все товары с "Тест" или "Test" в названии.

---

## 🎯 Схема версионирования

Используйте **семантическое версионирование** (SemVer):

```
MAJOR.MINOR.PATCH
  |     |     |
  |     |     └─ Исправления багов (2.0.0 → 2.0.1)
  |     └─────── Новые функции (2.0.0 → 2.1.0)
  └───────────── Критические изменения (2.0.0 → 3.0.0)
```

### Примеры:

- **2.0.0 → 2.0.1** - Исправлен баг с удалением товаров
- **2.0.1 → 2.1.0** - Добавлена новая функция (например, фильтры)
- **2.1.0 → 3.0.0** - Полная переработка интерфейса

---

## 🚀 Быстрое обновление (чеклист)

- [ ] Изменил `APP_VERSION` в `app.js`
- [ ] Обновил версию в `index.html` (3 скрипта)
- [ ] Обновил версию в `admin.html` (1 скрипт)
- [ ] Сохранил все файлы
- [ ] Перезапустил сервер
- [ ] Проверил в браузере (F12 → Console)

---

## 🔧 Дополнительные команды

### Полная очистка кеша вручную (в консоли браузера):

```javascript
// Очистить localStorage
localStorage.clear();

// Очистить cache storage
caches.keys().then(names => names.forEach(name => caches.delete(name)));

// Перезагрузить без кеша
location.reload(true);
```

### Проверить текущую версию (в консоли браузера):

```javascript
console.log('Версия:', APP_VERSION);
console.log('Сохраненная версия:', localStorage.getItem('appVersion'));
```

---

## ❓ FAQ

### Q: Почему товары все еще не удаляются?

**A:** Возможные причины:
1. **Кеш браузера** - обновите версию и перезагрузите
2. **Товары в БД** - проверьте через `node check-products.js`
3. **Права администратора** - проверьте `currentUser.is_admin`

### Q: Как удалить конкретный товар из БД?

**A:** Используйте скрипт:
```bash
node delete-test-products.js
```

Или вручную через SQLite:
```bash
sqlite3 shop.db
DELETE FROM orders WHERE product_id = 14;
DELETE FROM products WHERE id = 14;
.quit
```

### Q: Пользователь не видит обновления

**A:** Попросите пользователя:
1. Закрыть Mini App
2. Выйти из чата с ботом
3. Зайти снова
4. Открыть Mini App

### Q: Как откатить версию?

**A:** 
1. Измените `APP_VERSION` на старую версию
2. Обновите версии в HTML
3. Перезапустите сервер

---

## 📊 Мониторинг версий

Добавьте в админ-панель отображение версии:

```javascript
// В admin.js
console.log('Версия приложения:', APP_VERSION);
document.title = `Админ-панель v${APP_VERSION}`;
```

---

**Дата создания**: 2025-01-02  
**Текущая версия**: 2.0.0
