# 🔧 Исправление: Пропали админ права

## ❌ Проблема

После миграции на PostgreSQL админ права пропали, хотя Telegram ID добавлен в `.env` и Railway.

## 🎯 Причина

Код авторизации не проверял `ADMIN_TELEGRAM_IDS` из переменных окружения и не устанавливал `is_admin` при создании/обновлении пользователя.

## ✅ Что исправлено

### 1. Добавлена проверка ADMIN_TELEGRAM_IDS при авторизации

```javascript
// Проверяем, является ли пользователь админом
const adminIds = process.env.ADMIN_TELEGRAM_IDS ? 
    process.env.ADMIN_TELEGRAM_IDS.split(',').map(id => id.trim()) : 
    ADMIN_TELEGRAM_IDS;
const isAdmin = adminIds.includes(id.toString());

console.log('🔐 [AUTH] Проверка админ прав:', { 
    userId: id.toString(), 
    adminIds, 
    isAdmin 
});
```

### 2. Автоматическое обновление прав админа

```javascript
// Если пользователь уже есть, обновляем is_admin
if (user.is_admin !== isAdmin) {
    const updateUser = db.prepare('UPDATE users SET is_admin = $1 WHERE id = $2');
    await updateUser.run(isAdmin, user.id);
    user.is_admin = isAdmin;
    console.log('✅ [AUTH] Обновлены права админа:', isAdmin);
}
```

### 3. Добавлен async к функции авторизации

```javascript
app.post('/api/auth/telegram', async (req, res) => {  // ✅ async
    // ...
    let user = await getUser.get(id.toString());  // ✅ await
});
```

## 🔧 Настройка

### В .env файле:

```env
ADMIN_TELEGRAM_IDS=853232715,ВАШ_TELEGRAM_ID
```

### На Railway:

1. Откройте проект
2. Перейдите в **Variables**
3. Добавьте/обновите:
   ```
   ADMIN_TELEGRAM_IDS = 853232715,ВАШ_TELEGRAM_ID
   ```

## 📊 Как узнать свой Telegram ID

### Вариант 1: Через бота

1. Откройте [@userinfobot](https://t.me/userinfobot)
2. Нажмите `/start`
3. Скопируйте ваш ID

### Вариант 2: Через логи

1. Авторизуйтесь в приложении
2. Проверьте логи Railway
3. Найдите строку:
   ```
   👤 [SERVER AUTH] Данные пользователя: { id: 'ВАШ_ID', ... }
   ```

## 🚀 Как применить

```bash
git add .
git commit -m "Fix admin rights check from ADMIN_TELEGRAM_IDS v2.2.4"
git push
```

Railway автоматически задеплоит.

## ✅ Проверка

После деплоя:

1. Очистите кеш браузера (Ctrl+Shift+Delete)
2. Перезайдите в приложение
3. Проверьте логи Railway:
   ```
   🔐 [AUTH] Проверка админ прав: { 
       userId: 'ВАШ_ID', 
       adminIds: ['853232715', 'ВАШ_ID'], 
       isAdmin: true 
   }
   ✅ [AUTH] Обновлены права админа: true
   ```

4. В приложении должна появиться кнопка "Добавить услугу"
5. На карточках товаров должна быть кнопка "🗑️ Удалить"

## 🐛 Если не работает

### 1. Проверьте переменную окружения

В Railway → Variables:
```
ADMIN_TELEGRAM_IDS = 853232715,ВАШ_ID
```

⚠️ **Важно**: Без пробелов после запятой или используйте пробелы (код их обрезает).

### 2. Проверьте формат ID

ID должен быть **числом** без кавычек:
- ✅ Правильно: `853232715`
- ❌ Неправильно: `"853232715"`

### 3. Проверьте логи

Если в логах:
```
🔐 [AUTH] Проверка админ прав: { 
    userId: 'ВАШ_ID', 
    adminIds: ['853232715'], 
    isAdmin: false 
}
```

Значит ваш ID не в списке - проверьте `ADMIN_TELEGRAM_IDS`.

### 4. Очистите localStorage

В консоли браузера (F12):
```javascript
localStorage.clear();
location.reload();
```

## 📝 Что теперь работает

- ✅ Проверка админ прав из `ADMIN_TELEGRAM_IDS`
- ✅ Автоматическое обновление прав при входе
- ✅ Кнопка "Добавить услугу" для админов
- ✅ Кнопка "Удалить" на товарах для админов
- ✅ Логирование проверки прав

---

**Версия:** 2.2.4  
**Дата:** 03.11.2025  
**Статус:** ✅ Исправлено
