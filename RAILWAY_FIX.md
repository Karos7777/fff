# 🚂 Быстрое исправление ошибки Railway

## ❌ Ошибка

```
npm error Missing: pg@8.11.3 from lock file
ERROR: failed to build: exit code: 1
```

## ✅ Решение (2 варианта)

### Вариант 1: Обновить package-lock.json (Рекомендуется)

**Откройте CMD (не PowerShell!):**

```cmd
cd d:\projects\tg_magazin_bot
npm install
git add package-lock.json
git commit -m "Update package-lock.json for PostgreSQL"
git push
```

### Вариант 2: Удалить package-lock.json

```cmd
cd d:\projects\tg_magazin_bot
del package-lock.json
git add package-lock.json
git commit -m "Remove package-lock.json"
git push
```

Railway создаст новый lock файл автоматически.

## 🔧 Настройка PostgreSQL на Railway

### 1. Добавьте PostgreSQL базу данных

1. Откройте проект на Railway
2. Нажмите **"+ New"**
3. Выберите **"Database"**
4. Выберите **"Add PostgreSQL"**

Railway автоматически:
- Создаст базу данных
- Добавит переменную `DATABASE_URL`
- Подключит её к вашему сервису

### 2. Проверьте переменные окружения

В разделе **Variables** должна быть:

```
DATABASE_URL = postgresql://postgres:***@***:5432/railway
```

Если её нет, добавьте вручную или пересоздайте PostgreSQL.

### 3. Проверьте деплой

После push на GitHub, Railway автоматически:
1. Установит зависимости
2. Запустит сервер
3. Создаст таблицы

Проверьте логи - должно быть:

```
✅ База данных PostgreSQL инициализирована успешно
🚀 Сервер запущен на порту 10000
```

## 🎯 Проверка работы

### 1. Откройте ваше приложение

```
https://ваш-проект.up.railway.app
```

### 2. Проверьте API

```
https://ваш-проект.up.railway.app/api/products
```

Должен вернуть JSON с товарами (или пустой массив).

### 3. Проверьте базу данных

В Railway:
1. Откройте PostgreSQL сервис
2. Перейдите в **"Data"**
3. Выполните SQL:

```sql
SELECT * FROM users;
SELECT * FROM products;
```

## ⚠️ Важно

После миграции на PostgreSQL:

- ✅ Удалите `shop.db` из репозитория (добавьте в .gitignore)
- ✅ Убедитесь, что `DATABASE_URL` установлена
- ✅ Не коммитьте `.env` файл

## 📝 Чеклист

- [ ] Установлен пакет `pg`
- [ ] Обновлен `package-lock.json`
- [ ] Закоммичены изменения
- [ ] Добавлена PostgreSQL на Railway
- [ ] Проверена переменная `DATABASE_URL`
- [ ] Деплой успешен
- [ ] Таблицы созданы
- [ ] Приложение работает

---

**Готово!** Теперь ваше приложение использует PostgreSQL! 🎉
