# 💳 Интеграция системы платежей

## 📋 Обзор

Система поддерживает два типа платежей:
1. **Telegram Stars** - для цифровых услуг через Telegram
2. **Криптовалюты** - TON и USDT на TON блокчейне

## 🔧 Настройка

### 1. Установка зависимостей

```bash
npm install @ton/ton @ton/crypto node-cron axios uuid
```

### 2. Переменные окружения

Создайте файл `.env` на основе `.env.example`:

```env
# Обязательные
BOT_TOKEN=your_telegram_bot_token
TON_WALLET_ADDRESS=your_ton_wallet_address
TON_API_KEY=your_tonapi_key

# Опциональные
STARS_PROVIDER_TOKEN=your_stars_provider_token
JWT_SECRET=your_jwt_secret
```

### 3. Получение необходимых токенов

#### Telegram Bot Token
1. Создайте бота через [@BotFather](https://t.me/BotFather)
2. Получите токен командой `/newbot`
3. Включите платежи командой `/mybots` → выберите бота → Payments

#### TON API Key
1. Зарегистрируйтесь на [TonAPI.io](https://tonapi.io)
2. Создайте API ключ в личном кабинете
3. Добавьте ключ в переменные окружения

#### TON Кошелек
1. Создайте кошелек в [Tonkeeper](https://tonkeeper.com) или [TON Wallet](https://wallet.ton.org)
2. Скопируйте адрес кошелька (формат: EQC...)
3. Добавьте адрес в переменные окружения

## 🏗️ Архитектура

### Структура базы данных

```sql
-- Инвойсы (счета на оплату)
CREATE TABLE invoices (
  id INTEGER PRIMARY KEY,
  order_id INTEGER NOT NULL,
  user_id INTEGER NOT NULL,
  product_id INTEGER NOT NULL,
  
  amount REAL NOT NULL,
  currency TEXT NOT NULL, -- 'XTR', 'TON', 'USDT'
  status TEXT DEFAULT 'pending', -- pending, paid, expired, cancelled
  
  invoice_payload TEXT UNIQUE NOT NULL,
  
  -- Telegram Stars
  telegram_payment_charge_id TEXT,
  telegram_provider_payment_charge_id TEXT,
  
  -- Криптовалюты
  crypto_address TEXT,
  crypto_memo TEXT,
  crypto_tx_hash TEXT,
  crypto_confirmations INTEGER DEFAULT 0,
  
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  paid_at DATETIME,
  expires_at DATETIME
);

-- Транзакции (детальное логирование)
CREATE TABLE transactions (
  id INTEGER PRIMARY KEY,
  invoice_id INTEGER NOT NULL,
  
  type TEXT NOT NULL, -- 'stars', 'ton', 'usdt'
  status TEXT NOT NULL, -- 'pending', 'confirmed', 'failed'
  
  tx_hash TEXT,
  from_address TEXT,
  to_address TEXT,
  amount REAL NOT NULL,
  confirmations INTEGER DEFAULT 0,
  
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  confirmed_at DATETIME,
  
  metadata TEXT -- JSON с дополнительной информацией
);
```

### Компоненты системы

1. **PaymentService** (`payment-service.js`) - основная логика платежей
2. **API Endpoints** (в `server.js`) - REST API для фронтенда
3. **PaymentManager** (`public/payment.js`) - фронтенд интерфейс
4. **Cron Jobs** - автоматическая проверка платежей

## 🔄 Процесс оплаты

### Telegram Stars

1. Пользователь выбирает товар и нажимает "Заказать"
2. Создается заказ через `/api/orders`
3. Создается Stars инвойс через `/api/payments/stars/create-invoice`
4. Отображается интерфейс оплаты с кнопкой "Оплатить Stars"
5. Telegram отправляет `pre_checkout_query` на `/api/payments/stars/pre-checkout`
6. Система валидирует платеж и отвечает `answerPreCheckoutQuery`
7. После оплаты Telegram отправляет `successful_payment` на `/api/payments/stars/webhook`
8. Система обновляет статус заказа и отправляет уведомление пользователю

### Криптовалюты (TON/USDT)

1. Пользователь выбирает товар и криптовалюту
2. Создается крипто инвойс через `/api/payments/crypto/create-invoice`
3. Отображается адрес, memo и QR код для оплаты
4. Cron job каждые 2 минуты проверяет новые транзакции через TON API
5. При обнаружении платежа с правильным memo система:
   - Проверяет сумму и количество подтверждений
   - Обновляет статус заказа
   - Отправляет уведомление пользователю

## 🔒 Безопасность

### Валидация платежей

#### Telegram Stars
- Проверка `invoice_payload` на уникальность
- Валидация суммы и валюты
- Проверка срока действия инвойса
- Сверка пользователя

#### Криптовалюты
- Проверка реальной транзакции в блокчейне
- Сравнение адреса получателя
- Валидация суммы (с допуском на комиссии)
- Проверка memo/comment
- Минимальное количество подтверждений

### Защита от атак

1. **Дубликаты платежей** - уникальные payload и проверка статуса
2. **Подмена сумм** - строгая валидация на сервере
3. **Фальшивые транзакции** - проверка через официальные API
4. **Replay атаки** - временные метки и одноразовые payload

## 📡 API Endpoints

### Создание инвойсов

```javascript
// Stars
POST /api/payments/stars/create-invoice
{
  "orderId": 123,
  "productId": 456,
  "amount": 100,
  "description": "Название товара"
}

// Криптовалюты
POST /api/payments/crypto/create-invoice
{
  "orderId": 123,
  "productId": 456,
  "amount": 1.5,
  "currency": "TON" // или "USDT"
}
```

### Проверка статуса

```javascript
GET /api/payments/status/:payload
```

### Webhooks

```javascript
// Pre-checkout для Stars
POST /api/payments/stars/pre-checkout

// Successful payment для Stars  
POST /api/payments/stars/webhook

// Ручная проверка криптоплатежей (для админов)
POST /api/payments/crypto/check
```

## 🎨 Фронтенд интеграция

### Использование PaymentManager

```javascript
// Показать опции оплаты
paymentManager.showPaymentOptions(orderId, productId, productName, price);

// Проверить статус платежа
const status = await paymentManager.checkPaymentStatus(payload);
```

### Стили

Все стили находятся в `payment.css` с поддержкой:
- Адаптивного дизайна
- Темной темы
- Анимаций и переходов
- Доступности

## 🤖 Автоматизация

### Cron Jobs

```javascript
// Проверка криптоплатежей каждые 2 минуты
cron.schedule('*/2 * * * *', async () => {
  await paymentService.checkCryptoPayments();
});

// Очистка просроченных инвойсов каждые 10 минут
cron.schedule('*/10 * * * *', () => {
  paymentService.cancelExpiredInvoices();
});
```

### Уведомления

После успешной оплаты система автоматически:
1. Обновляет статус заказа в БД
2. Отправляет уведомление пользователю в Telegram
3. Логирует транзакцию для аудита

## 🧪 Тестирование

### Локальное тестирование

1. Запустите сервер: `npm start`
2. Откройте админ панель: `http://localhost:10000/admin`
3. Создайте тестовый товар
4. Протестируйте процесс заказа

### Тестирование в Telegram

1. Создайте тестового бота
2. Настройте Telegram Mini App
3. Протестируйте Stars платежи в тестовом режиме
4. Для крипто используйте тестовые сети

## 🚀 Деплой

### Переменные окружения на Render

```env
BOT_TOKEN=your_production_bot_token
TON_WALLET_ADDRESS=your_production_wallet
TON_API_KEY=your_production_api_key
JWT_SECRET=your_production_jwt_secret
PORT=10000
```

### Webhook настройка

Для Telegram Stars настройте webhook:
```
https://your-domain.com/api/payments/stars/webhook
```

## 📊 Мониторинг

### Логирование

Система логирует:
- Создание инвойсов
- Успешные платежи  
- Ошибки валидации
- Проблемы с API

### Метрики

Отслеживайте:
- Конверсию платежей
- Время обработки
- Количество ошибок
- Популярные способы оплаты

## 🔧 Troubleshooting

### Частые проблемы

1. **"Cannot find module 'better-sqlite3'"**
   - Решение: `npm install better-sqlite3`

2. **"TON API не отвечает"**
   - Проверьте API ключ
   - Убедитесь в доступности tonapi.io

3. **"Платеж не обнаруживается"**
   - Проверьте правильность memo
   - Убедитесь в достаточном количестве подтверждений

4. **"Stars платеж не проходит"**
   - Проверьте настройки бота в BotFather
   - Убедитесь в правильности webhook URL

### Отладка

Включите детальное логирование:
```javascript
console.log('Payment debug info:', {
  invoice,
  transaction,
  confirmations
});
```

## 📚 Дополнительные ресурсы

- [Telegram Bot API - Payments](https://core.telegram.org/bots/payments)
- [TON Documentation](https://ton.org/docs)
- [TonAPI Documentation](https://tonapi.io/docs)
- [Telegram Mini Apps](https://core.telegram.org/bots/webapps)

## 🤝 Поддержка

При возникновении проблем:
1. Проверьте логи сервера
2. Убедитесь в правильности переменных окружения
3. Протестируйте API endpoints через Postman
4. Обратитесь к документации TON и Telegram
