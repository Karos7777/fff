# 📦 Модульная структура приложения

## Версия 4.0.0

Приложение теперь разбито на модули для улучшения поддержки и масштабируемости.

## 📁 Структура модулей

### `auth.js` - Аутентификация
- `initAuthInterceptor()` - Перехватчик fetch с автоматическим добавлением токенов
- `autoAuth()` - Автоматическая аутентификация через Telegram WebApp
- `getCurrentUser()` - Получение текущего пользователя
- `isAuthenticated()` - Проверка аутентификации
- `logout()` - Выход из системы

### `ui.js` - Пользовательский интерфейс
- `showError(message)` - Показать модальное окно ошибки
- `showSuccess(message)` - Показать уведомление об успехе
- `showLoading(message)` - Показать индикатор загрузки
- `hideLoading()` - Скрыть индикатор загрузки
- `showTONPayment(order)` - Показать модальное окно оплаты TON
- `closePaymentModal()` - Закрыть модальное окно оплаты
- `initializeCopyButtons()` - Инициализация кнопок копирования

### `payments.js` - Платежи
- `createOrder(productId, paymentMethod)` - Создать заказ
- `payWithTON(productId)` - Оплата через TON
- `payWithUSDT(productId)` - Оплата через USDT
- `payWithStars(productId)` - Оплата через Stars (временно отключено)
- `checkOrderStatus(orderId)` - Проверить статус заказа
- `processPayment(orderData, paymentMethod)` - Обработка платежа
- `isStarsPaymentAvailable()` - Проверка доступности Stars

### `main.js` - Главный модуль
- Инициализация приложения
- Глобальные обработчики ошибок
- Защита от undefined URL
- Экспорт функций в глобальную область

## 🎨 Стили

### `css/payment-modal.css` - Модальное окно оплаты
- Адаптивный дизайн
- Кнопки копирования с визуальным фидбеком
- Анимации появления/скрытия
- Поддержка мобильных устройств

## 🚀 Использование

### Подключение в HTML
```html
<!-- Стили -->
<link rel="stylesheet" href="css/payment-modal.css">

<!-- Модули -->
<script type="module" src="js/main.js"></script>
```

### Использование функций
```javascript
// Функции доступны глобально
payWithTON(productId);
payWithUSDT(productId);
showError('Ошибка!');
showSuccess('Успешно!');
```

### Импорт в модулях
```javascript
import { showError, showSuccess } from './ui.js';
import { payWithTON } from './payments.js';
import { getCurrentUser } from './auth.js';
```

## ✨ Новые возможности

### Модальное окно оплаты TON
- **Копирование данных** - одним кликом
- **Понятные инструкции** - пошаговое руководство
- **Прямая интеграция** - кнопка открытия Telegram Wallet
- **Валидация** - предотвращение ошибок пользователя

### Улучшенная обработка ошибок
- Глобальные обработчики
- Блокировка undefined URL
- Красивые модальные окна
- Автоматическое закрытие уведомлений

## 🔧 Разработка

### Добавление нового модуля
1. Создайте файл в папке `js/`
2. Экспортируйте функции через `export`
3. Импортируйте в `main.js`
4. При необходимости экспортируйте в `window` для глобального доступа

### Обновление версии
Измените константу `APP_VERSION` в `main.js`

## 📋 Миграция со старой структуры

**Удалено:**
- `app.js` (2319 строк) - разбит на модули

**Добавлено:**
- `js/auth.js` - аутентификация
- `js/ui.js` - интерфейс
- `js/payments.js` - платежи
- `js/main.js` - главный модуль
- `css/payment-modal.css` - стили

**Совместимость:**
Все функции остаются доступны глобально через `window`, поэтому старый код продолжит работать.

## 🎯 Преимущества

- ✅ **Модульность** - легче поддерживать
- ✅ **Читаемость** - понятная структура
- ✅ **Масштабируемость** - просто добавлять функции
- ✅ **Переиспользование** - импорт только нужного
- ✅ **Отладка** - проще находить ошибки
