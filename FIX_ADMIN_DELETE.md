# 🔧 Исправление: Удаление товаров для админов

## Проблема
Кнопка "Удалить" не отображалась для администраторов в мини-апп приложении.

## Причина
Несоответствие форматов данных:
- Сервер отправлял `isAdmin` (camelCase)
- Клиент проверял `is_admin` (snake_case)

## Решение

### 1. Исправлено на сервере (server.js)
Теперь сервер отправляет **оба формата**:
```javascript
user: {
    is_admin: user.is_admin,  // snake_case
    isAdmin: user.is_admin     // camelCase
}
```

### 2. Исправлено на клиенте (app.js)
- Добавлено обновление `window.currentUser` при авторизации
- Добавлено логирование статуса админа
- Добавлено обновление при восстановлении сессии

## Как проверить

### Шаг 1: Откройте приложение
```
http://localhost:10000
```

### Шаг 2: Откройте консоль (F12)
Вы должны увидеть:
```
✅ [AUTH] is_admin: true isAdmin: true
```
или
```
✅ [RESTORE] is_admin: true isAdmin: true
```

### Шаг 3: Проверьте кнопку удаления
Если вы админ, на каждой карточке товара должна быть кнопка:
```
🗑️ Удалить
```

### Шаг 4: Попробуйте удалить товар
1. Нажмите "🗑️ Удалить" на любом товаре
2. Подтвердите удаление
3. Товар должен исчезнуть из списка

## Проверка статуса админа

### В консоли браузера выполните:
```javascript
console.log('currentUser:', currentUser);
console.log('is_admin:', currentUser?.is_admin);
console.log('isAdmin:', currentUser?.isAdmin);
```

Если вы админ, оба значения должны быть `true`.

## Как стать админом

### Вариант 1: Через базу данных
```bash
sqlite3 shop.db
UPDATE users SET is_admin = 1 WHERE telegram_id = 'ВАШ_TELEGRAM_ID';
.quit
```

### Вариант 2: Через .env файл
Добавьте ваш Telegram ID в `ADMIN_TELEGRAM_IDS`:
```
ADMIN_TELEGRAM_IDS=853232715,ВАШ_TELEGRAM_ID
```

Затем перезапустите сервер.

## Логи для отладки

При авторизации вы должны видеть:
```
👤 [AUTH] Сохранение токена и данных пользователя...
✅ [AUTH] Данные сохранены. Текущий пользователь: {...}
✅ [AUTH] is_admin: true isAdmin: true
```

При восстановлении сессии:
```
✅ [RESTORE] Восстановлен пользователь: {...}
✅ [RESTORE] is_admin: true isAdmin: true
```

При рендеринге товаров:
```
📦 [LOAD] Загружено товаров: 10
```

## Если не работает

### 1. Очистите кеш браузера
```
Ctrl + Shift + Delete
или
Ctrl + F5
```

### 2. Очистите localStorage
В консоли браузера:
```javascript
localStorage.clear();
location.reload();
```

### 3. Проверьте базу данных
```bash
sqlite3 shop.db
SELECT telegram_id, is_admin FROM users;
.quit
```

### 4. Проверьте .env файл
```bash
cat .env | grep ADMIN
```

## Версия
**2.1.2** - Исправлено несоответствие форматов данных

## Дата
03.11.2025
