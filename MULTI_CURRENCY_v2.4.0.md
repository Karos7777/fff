# ✅ Поддержка множественных валют v2.4.0

## 🎯 Что добавлено

### 1. Поддержка трёх валют для товаров

Теперь при создании товара можно указать цены в:
- 💎 **TON** (минимум 0.001)
- 💵 **USDT** (минимум 0.001)
- ⭐ **Telegram Stars** (минимум 1)

### 2. Гибкая система цен

- ✅ Можно заполнить **одну** валюту (например, только TON)
- ✅ Можно заполнить **две** валюты (например, TON и USDT)
- ✅ Можно заполнить **все три** валюты
- ❌ Нельзя создать товар **без цен** - хотя бы одна обязательна

### 3. Снижен минимум до 0.001

**Было:** минимум 0.01 TON/USDT  
**Стало:** минимум 0.001 TON/USDT

Это позволяет создавать товары с микро-ценами для тестирования.

---

## 📋 Изменения в коде

### 1. База данных (server.js)

**Новые колонки в таблице `products`:**
```sql
price_ton DECIMAL(10,4),      -- цена в TON (до 4 знаков после запятой)
price_usdt DECIMAL(10,4),     -- цена в USDT (до 4 знаков после запятой)  
price_stars INTEGER           -- цена в Stars (целое число)
```

**Миграция добавлена автоматически:**
```javascript
ALTER TABLE products 
ADD COLUMN IF NOT EXISTS price_ton DECIMAL(10,4),
ADD COLUMN IF NOT EXISTS price_usdt DECIMAL(10,4),
ADD COLUMN IF NOT EXISTS price_stars INTEGER
```

### 2. Форма создания товара (index.html)

**Было:**
```html
<div class="form-group">
    <label>Цена *</label>
    <input type="number" name="price" step="0.01" required>
</div>
```

**Стало:**
```html
<div class="form-row">
    <div class="form-group">
        <label>💎 TON</label>
        <input type="number" name="price_ton" step="0.001" min="0" placeholder="0.001">
    </div>
    <div class="form-group">
        <label>💵 USDT</label>
        <input type="number" name="price_usdt" step="0.001" min="0" placeholder="0.001">
    </div>
    <div class="form-group">
        <label>⭐ Stars</label>
        <input type="number" name="price_stars" step="1" min="0" placeholder="1">
    </div>
</div>
```

### 3. Валидация (app.js)

**Добавлена проверка:**
```javascript
const priceTon = parseFloat(formData.get('price_ton')) || 0;
const priceUsdt = parseFloat(formData.get('price_usdt')) || 0;
const priceStars = parseInt(formData.get('price_stars')) || 0;

if (priceTon === 0 && priceUsdt === 0 && priceStars === 0) {
    showError('Заполните хотя бы одну цену (TON, USDT или Stars)');
    return;
}
```

### 4. Обработка на сервере (server.js)

**Парсинг полей:**
```javascript
const priceTon = req.body.price_ton ? parseFloat(req.body.price_ton) : null;
const priceUsdt = req.body.price_usdt ? parseFloat(req.body.price_usdt) : null;
const priceStars = req.body.price_stars ? parseInt(req.body.price_stars) : null;
```

**SQL INSERT:**
```javascript
INSERT INTO products (
  name, description, price, 
  price_ton, price_usdt, price_stars,  -- Новые поля
  category, stock, infinite_stock, image_url, is_active
)
VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, true)
```

### 5. Конвертация типов (server.js)

**GET /api/products:**
```javascript
const result = products.map(p => ({
  ...p,
  price: parseFloat(p.price),
  price_ton: p.price_ton ? parseFloat(p.price_ton) : null,
  price_usdt: p.price_usdt ? parseFloat(p.price_usdt) : null,
  price_stars: p.price_stars ? parseInt(p.price_stars) : null,
  rating: parseFloat(ratingMap[p.id]?.avg_rating) || 0,
  reviewsCount: parseInt(ratingMap[p.id]?.reviews_count) || 0
}));
```

### 6. Оплата (payment.js)

**Исправлено:**
- ✅ Ключ токена: `'token'` → `'authToken'`
- ✅ Минимум: `0.01` → `0.001`

```javascript
// До:
Math.max(price / 100, 0.01).toFixed(4)

// После:
Math.max(price / 100, 0.001).toFixed(4)
```

---

## 🧪 Как использовать

### 1. Создание товара с одной ценой (TON)

1. Откройте форму "➕ Добавить услугу"
2. Заполните:
   - Название: `Тестовая услуга`
   - TON: `0.001`
   - (Остальные цены оставьте пустыми)
3. Нажмите "Сохранить"
4. **Товар создастся!** ✅

### 2. Создание товара с двумя ценами (TON + USDT)

1. Заполните:
   - Название: `Премиум услуга`
   - TON: `0.005`
   - USDT: `0.5`
3. Нажмите "Сохранить"
4. **Товар создастся!** ✅

### 3. Создание товара со всеми ценами

1. Заполните:
   - Название: `VIP услуга`
   - TON: `0.01`
   - USDT: `1.0`
   - Stars: `100`
3. Нажмите "Сохранить"
4. **Товар создастся!** ✅

### 4. Попытка создать товар без цен ❌

1. Заполните только название
2. Оставьте все цены пустыми
3. Нажмите "Сохранить"
4. **Ошибка:** "Заполните хотя бы одну цену (TON, USDT или Stars)"

---

## 📊 Примеры использования

### Микро-товары (для тестирования)

```
Название: Тестовая услуга 1
TON: 0.001
USDT: (пусто)
Stars: (пусто)
```

### Доступные товары

```
Название: Консультация 30 мин
TON: 0.01
USDT: 1.0
Stars: 100
```

### Премиум товары

```
Название: Разработка сайта
TON: 1.5
USDT: 150
Stars: 15000
```

---

## ⚙️ Обратная совместимость

### Поле `price` (устаревшее)

Старое поле `price` остаётся для обратной совместимости и заполняется автоматически:

```javascript
// Берем первую ненулевую цену
const compatPrice = priceTon || priceUsdt || (priceStars / 100) || 1;
formData.set('price', compatPrice);
```

**В форме:**
```html
<div class="form-group">
    <label>Цена (устаревшее поле, для совместимости)</label>
    <input type="number" name="price" value="1" readonly>
    <small>Это поле заполняется автоматически</small>
</div>
```

---

## 🔄 Миграция существующих товаров

Старые товары с только `price` будут продолжать работать:

```
Товар до миграции:
{
  id: 1,
  name: "Старый товар",
  price: 10.00,
  price_ton: null,    // Новые поля = null
  price_usdt: null,
  price_stars: null
}
```

Клиент корректно обработает `null` значения.

---

## 🎨 UI/UX

### Плейсхолдеры

Показывают минимальные значения:
- TON: `0.001`
- USDT: `0.001`
- Stars: `1`

### Подсказки

```
💰 Цены (заполните хотя бы одну)
Минимум: 0.001 TON/USDT или 1 Star
```

### Валидация

- ❌ Все пусто → "Заполните хотя бы одну цену"
- ✅ Одна заполнена → Товар создаётся
- ✅ Две заполнены → Товар создаётся
- ✅ Все заполнены → Товар создаётся

---

## 🚀 Следующие шаги

### 1. Поддержка Stars в платежах

В будущем будет добавлена полная поддержка оплаты через Telegram Stars.

**Сейчас:**
- ✅ Поле `price_stars` сохраняется в БД
- ⏳ Оплата Stars (в разработке)

### 2. Динамическое отображение доступных методов оплаты

На карточке товара будут показываться только те способы оплаты, для которых указана цена:

```javascript
// Если у товара price_ton = 0.01 и price_stars = 100:
// Показать: TON, Stars
// Скрыть: USDT (не указана цена)
```

### 3. Конвертация валют

В будущем можно добавить автоматическую конвертацию:

```javascript
// Админ указал только TON: 0.01
// Система автоматически рассчитывает:
// - USDT: ~1.0 (курс 1 TON = 100 USDT)
// - Stars: ~100 (курс 1 TON = 10000 Stars)
```

---

## 📝 Изменённые файлы

1. **`server.js`**
   - Добавлены колонки `price_ton`, `price_usdt`, `price_stars`
   - Миграция для существующих БД
   - Парсинг и сохранение новых полей
   - Конвертация типов при GET

2. **`public/index.html`**
   - Форма с тремя полями цен
   - Плейсхолдеры и подсказки
   - Версия скриптов: `2.4.0`

3. **`public/app.js`**
   - Валидация: хотя бы одна цена
   - Автозаполнение `price` для совместимости
   - Версия: `2.4.0`

4. **`public/payment.js`**
   - Исправлен ключ токена: `authToken`
   - Минимум: `0.001`

5. **`package.json`**
   - Версия: `2.4.0`

---

**Версия:** 2.4.0  
**Дата:** 3 ноября 2025  
**Статус:** ✅ Реализовано

**Поддержка множественных валют работает!** 🎉

### Что работает:
- ✅ Форма с тремя валютами
- ✅ Валидация (хотя бы одна цена)
- ✅ Минимум 0.001 для TON/USDT
- ✅ Сохранение в БД
- ✅ Отображение на клиенте
- ✅ Обратная совместимость
- ✅ Исправлен токен в payment.js

### В разработке:
- ⏳ Полная интеграция оплаты Stars
- ⏳ Динамическое отображение доступных методов оплаты
- ⏳ Автоматическая конвертация валют
