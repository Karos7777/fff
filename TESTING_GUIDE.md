# 🧪 Руководство по тестированию TON оплаты и автоматической выдачи

## 📋 Что уже работает

✅ **Админ-панель:**
- Добавление товаров
- Редактирование товаров  
- **Удаление товаров** (кнопка 🗑️)
- Загрузка файлов

✅ **TON Polling:**
- Автоматическая проверка каждые 8 секунд
- Подтверждение оплаты по комментарию
- Автоматическая выдача товара

✅ **Кнопка "Открыть в кошельке":**
- Открывает TON кошелек
- Предзаполняет адрес, сумму, комментарий

---

## 🎯 Как протестировать полный цикл

### **Шаг 1: Добавьте тестовый товар через админку**

1. Откройте админ-панель: `https://ваш-домен.railway.app/admin.html`
2. Перейдите на вкладку **"Товары"**
3. Нажмите **"Добавить товар"**
4. Заполните форму:
   ```
   Название: 🎁 Премиум Гайд (ТЕСТ)
   Описание: Тестовый товар для проверки автоматической выдачи
   Цена TON: 0.1
   Цена USDT: 0.5
   Цена Stars: 10
   Категория: Гайды
   Файл товара: Загрузите файл /files/premium-guide.pdf
   Остаток: 999
   ✅ Активный товар
   ✅ Бесконечный остаток
   ```
5. Нажмите **"Сохранить"**
6. ✅ Товар появится в списке

---

### **Шаг 2: Купите товар**

1. Откройте магазин: `https://ваш-домен.railway.app/`
2. Найдите товар "🎁 Премиум Гайд (ТЕСТ)"
3. Нажмите **"Купить"**
4. Выберите способ оплаты: **TON**
5. Откроется модальное окно с:
   - QR кодом
   - Кнопкой "💳 Открыть в кошельке"
   - Данными для копирования

---

### **Шаг 3: Оплатите**

**Вариант 1: Через кнопку**
1. Нажмите **"💳 Открыть в кошельке"**
2. Откроется TON кошелек (Tonkeeper/Tonhub)
3. Проверьте:
   - ✅ Адрес: `UQCm27jo_LGzzwx49_niSXqEz9ZRRTyxJxa-yD89Wnxb13fx`
   - ✅ Сумма: `0.1 TON`
   - ✅ Комментарий: `order_1762689810_abc123` (автоматически)
4. Подтвердите оплату

**Вариант 2: Через QR код**
1. Отсканируйте QR код камерой телефона
2. Откроется кошелек с предзаполненными данными
3. Подтвердите оплату

**Вариант 3: Вручную**
1. Скопируйте адрес кошелька
2. Скопируйте сумму
3. **ОБЯЗАТЕЛЬНО** скопируйте комментарий
4. Отправьте в своем кошельке

---

### **Шаг 4: Дождитесь подтверждения**

1. Подождите **8-30 секунд**
2. В логах сервера появится:
   ```
   ✅ [TON POLLING] ОПЛАТА ЗАСЧИТАНА! Заказ #123
   🎁 [TON POLLING] Автоматическая выдача товара
      📦 Товар: 🎁 Премиум Гайд (ТЕСТ)
      👤 Пользователь: username (ID: 853232715)
      📁 Файл доступен: /files/premium-guide.pdf
   ```

---

### **Шаг 5: Получите товар**

1. Откройте **"📦 Мои заказы"** на главной странице
2. Найдите свой заказ
3. Статус должен быть: **"✅ Оплачен"**
4. Нажмите **"📥 Скачать файл"**
5. ✅ Файл `premium-guide.pdf` скачается

---

## 🗑️ Как удалить товар (админ)

1. Откройте админ-панель
2. Перейдите на вкладку **"Товары"**
3. Найдите нужный товар в таблице
4. Нажмите кнопку **🗑️** (Удалить)
5. Подтвердите удаление
6. ✅ Товар удалится из базы данных

**Примечание:** Нельзя удалить товар, если есть активные заказы (pending/paid)

---

## 🔧 Если что-то не работает

### **Проблема 1: Кошелек не открывается**

**Решение:**
- Убедитесь, что у вас установлен TON кошелек (Tonkeeper/Tonhub)
- Попробуйте отсканировать QR код
- Или скопируйте данные вручную

### **Проблема 2: Оплата не подтверждается**

**Проверьте:**
1. Правильно ли указан комментарий (payload)
2. Достаточно ли суммы (минимум 90% от указанной)
3. Логи сервера - есть ли ошибки

**В логах должно быть:**
```
[TON POLLING] Проверяем 1 заказов: #123
[TON POLLING] Найдено 5 транзакций
[TON POLLING] TX 1: 0.100000000 TON → UQCm27... | msg: "order_..."
   ✅ НАЙДЕНО! payload: "order_..." | сумма: 0.100000000 TON
✅ [TON POLLING] ОПЛАТА ЗАСЧИТАНА!
```

### **Проблема 3: Файл не скачивается**

**Проверьте:**
1. Путь к файлу в базе данных: `/files/premium-guide.pdf`
2. Файл существует: `d:\projects\tg_magazin_bot\files\premium-guide.pdf`
3. Статус заказа: должен быть `completed`

**SQL запрос для проверки:**
```sql
SELECT o.id, o.status, p.name, p.file_url 
FROM orders o 
JOIN products p ON o.product_id = p.id 
WHERE o.user_id = (SELECT id FROM users WHERE telegram_id = '853232715')
ORDER BY o.created_at DESC 
LIMIT 5;
```

---

## 📊 Полезные SQL запросы

### Проверить все заказы
```sql
SELECT 
    o.id, 
    o.status, 
    o.total_amount, 
    o.payment_method,
    o.invoice_payload,
    p.name as product_name,
    p.file_url,
    u.username
FROM orders o
JOIN products p ON o.product_id = p.id
JOIN users u ON o.user_id = u.id
ORDER BY o.created_at DESC
LIMIT 10;
```

### Проверить pending заказы
```sql
SELECT 
    i.id, 
    i.order_id, 
    i.amount, 
    i.status,
    o.invoice_payload
FROM invoices i
JOIN orders o ON i.order_id = o.id
WHERE i.status = 'pending' AND i.currency = 'TON';
```

### Проверить товары с файлами
```sql
SELECT id, name, file_url, is_active 
FROM products 
WHERE file_url IS NOT NULL 
ORDER BY created_at DESC;
```

---

## ✅ Чеклист успешного теста

- [ ] Товар добавлен через админку
- [ ] Файл загружен и путь сохранен
- [ ] Товар виден на главной странице
- [ ] Кнопка "Купить" работает
- [ ] Модальное окно оплаты открывается
- [ ] Кнопка "Открыть в кошельке" работает
- [ ] Кошелек открывается с предзаполненными данными
- [ ] Оплата отправлена
- [ ] В логах появилось "ОПЛАТА ЗАСЧИТАНА"
- [ ] Статус заказа изменился на "completed"
- [ ] Заказ виден в "Мои заказы"
- [ ] Кнопка "Скачать файл" появилась
- [ ] Файл успешно скачался

---

## 🎉 Готово!

Если все пункты выполнены - ваша система автоматической выдачи работает идеально! 🚀
