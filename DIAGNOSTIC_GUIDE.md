# 🔍 Руководство по диагностике проблем

## Обзор

Этот файл содержит инструкции по диагностике двух основных проблем:
1. **Товары не удаляются** из интерфейса после удаления
2. **Пользователь не отображается** при входе в Mini App

## 📋 Подготовка к диагностике

### Шаг 1: Откройте DevTools

1. Откройте ваше приложение в браузере (Telegram Web или обычный браузер)
2. Нажмите **F12** или **Ctrl+Shift+I** (Windows/Linux) / **Cmd+Option+I** (Mac)
3. Вы увидите панель разработчика

### Шаг 2: Настройте консоль

1. Перейдите на вкладку **Console**
2. Убедитесь, что включены все уровни логов (Info, Warnings, Errors)
3. Очистите консоль (иконка 🚫 или Ctrl+L)

---

## 🗑️ Диагностика проблемы удаления товаров

### Что проверять:

#### 1. Откройте консоль и попробуйте удалить товар

Вы должны увидеть последовательность логов с эмодзи:

```
🗑️ [DELETE] Начало удаления товара, ID: 5
🗑️ [DELETE] Текущий список товаров: 10 шт.
🗑️ [DELETE] Найден товар: {id: 5, name: "...", ...}
🗑️ [DELETE] Токен авторизации: Есть
🗑️ [DELETE] Отправка DELETE запроса на: /api/admin/products/5
🗑️ [DELETE] Ответ сервера - Статус: 200 OK
✅ [DELETE] Успешный ответ от сервера: {...}
🗑️ [DELETE] Очистка кеша браузера...
✅ [DELETE] Товар удален из локального массива. Осталось: 9 шт.
🗑️ [DELETE] Перерисовка UI...
📦 [LOAD] Начало загрузки товаров, forceReload: true
✅ [LOAD] Загружено товаров: 9
✅ [DELETE] Удаление завершено успешно!
```

#### 2. Проверьте Network (вкладка Network в DevTools)

1. Перейдите на вкладку **Network**
2. Попробуйте удалить товар
3. Найдите запрос `DELETE /api/admin/products/{ID}`
4. Проверьте:
   - **Status Code**: должен быть **200 OK**
   - **Response**: должен содержать `{"success": true, "message": "Товар успешно удален"}`
   - **Request Headers**: должен содержать `Authorization: Bearer ...`

#### 3. Проверьте Application → LocalStorage

1. Перейдите на вкладку **Application**
2. Откройте **Local Storage** → ваш домен
3. Проверьте наличие ключа `authToken`

### ❌ Возможные ошибки и решения:

| Ошибка в консоли | Причина | Решение |
|------------------|---------|---------|
| `❌ [DELETE] Товар не найден в локальном массиве` | Товар не загружен на клиенте | Перезагрузите страницу |
| `❌ [DELETE] Токен авторизации: Отсутствует` | Нет авторизации | Выйдите и войдите снова |
| `❌ [DELETE] Ответ сервера - Статус: 401` | Токен недействителен | Выйдите и войдите снова |
| `❌ [DELETE] Ответ сервера - Статус: 403` | Нет прав администратора | Проверьте, что вы админ |
| `❌ [DELETE] Ответ сервера - Статус: 500` | Ошибка на сервере | Проверьте логи сервера |
| Товар удаляется, но появляется снова | Проблема с кешем или БД | См. ниже "Глубокая диагностика" |

---

## 👤 Диагностика проблемы отображения пользователя

### Что проверять:

#### 1. Откройте консоль при загрузке приложения

Вы должны увидеть:

```
🔐 [AUTH] ========== НАЧАЛО АВТОМАТИЧЕСКОЙ АВТОРИЗАЦИИ ==========
🔍 [AUTH] Отладка Telegram Web App:
🔍 [AUTH] - window.Telegram: {...}
🔍 [AUTH] - WebApp: {...}
🔍 [AUTH] - initData: "..."
🔍 [AUTH] - initDataUnsafe: {user: {...}}
✅ [AUTH] Авторизуем пользователя Telegram: {id: 123456, username: "..."}
👤 [AUTH] Начало аутентификации пользователя
👤 [AUTH] Отправка на /api/auth: {...}
👤 [AUTH] Ответ сервера - Статус: 200 OK
✅ [AUTH] Данные сохранены. Текущий пользователь: {...}
👤 [AUTH] Отображение основного контента...
📦 [LOAD] Начало загрузки товаров...
✅ [LOAD] Загружено товаров: 10
✅ [AUTH] Аутентификация завершена успешно!
🔐 [AUTH] ========== КОНЕЦ АВТОМАТИЧЕСКОЙ АВТОРИЗАЦИИ ==========
```

#### 2. Проверьте Network

1. Перейдите на вкладку **Network**
2. Найдите запрос `POST /api/auth`
3. Проверьте:
   - **Status Code**: должен быть **200 OK**
   - **Request Payload**: должен содержать `telegram_id`, `username`, `first_name`, `last_name`
   - **Response**: должен содержать `{"token": "...", "user": {...}}`

#### 3. Проверьте Application → LocalStorage

Должны быть ключи:
- `authToken`: JWT токен
- `currentUser`: JSON с данными пользователя

### ❌ Возможные ошибки и решения:

| Ошибка в консоли | Причина | Решение |
|------------------|---------|---------|
| `⚠️ [AUTH] Данные пользователя Telegram не найдены` | Не в Telegram или нет initData | Откройте через Telegram Bot |
| `❌ [AUTH] Ответ сервера - Статус: 500` | Ошибка на сервере | Проверьте логи сервера |
| `❌ [AUTH] Ошибка авторизации` | Проблема с сервером | Проверьте, работает ли сервер |
| Пользователь не отображается, но токен есть | Проблема с UI | Проверьте `showUserInfo()` |

---

## 🔬 Глубокая диагностика

### Если товар удаляется, но появляется снова:

1. **Проверьте базу данных на сервере**:
   ```bash
   # Подключитесь к серверу и выполните:
   sqlite3 shop.db "SELECT * FROM products WHERE id = {ID};"
   ```
   Если товар есть в БД - проблема на сервере.

2. **Проверьте логи сервера**:
   - Найдите строку с `DELETE /api/admin/products/{ID}`
   - Проверьте, выполнился ли SQL запрос `DELETE FROM products WHERE id = ?`

3. **Проверьте кеш браузера**:
   - Application → Cache Storage
   - Очистите все кеши вручную
   - Попробуйте в режиме Incognito

### Если пользователь не отображается:

1. **Проверьте Telegram Web App API**:
   ```javascript
   // В консоли выполните:
   window.Telegram.WebApp.initDataUnsafe
   ```
   Должен вернуть объект с `user`.

2. **Проверьте DOM**:
   ```javascript
   // В консоли выполните:
   document.getElementById('userInfo').style.display
   document.getElementById('userName').textContent
   ```

3. **Проверьте currentUser**:
   ```javascript
   // В консоли выполните:
   currentUser
   ```
   Должен вернуть объект пользователя.

---

## 📊 Сбор информации для отчета

Если проблема не решается, соберите следующую информацию:

### 1. Скриншоты консоли
- Полный вывод консоли при возникновении ошибки
- Вкладка Network с запросами

### 2. Информация о среде
- Браузер и версия
- ОС
- Открыто через Telegram Web или обычный браузер?

### 3. Логи сервера
- Последние 50 строк логов при возникновении ошибки

### 4. Данные из LocalStorage
```javascript
// Выполните в консоли:
console.log('authToken:', localStorage.getItem('authToken'));
console.log('currentUser:', localStorage.getItem('currentUser'));
```

### 5. Состояние приложения
```javascript
// Выполните в консоли:
console.log('products:', products.length);
console.log('currentUser:', currentUser);
console.log('Telegram WebApp:', window.Telegram?.WebApp);
```

---

## 🛠️ Быстрые исправления

### Проблема: Товары не удаляются

**Решение 1**: Очистите кеш браузера
```javascript
// Выполните в консоли:
caches.keys().then(names => names.forEach(name => caches.delete(name)));
location.reload();
```

**Решение 2**: Выйдите и войдите снова
```javascript
// Выполните в консоли:
localStorage.clear();
location.reload();
```

**Решение 3**: Проверьте права администратора
```javascript
// Выполните в консоли:
console.log('Is admin:', currentUser?.is_admin);
```

### Проблема: Пользователь не отображается

**Решение 1**: Принудительно отобразите пользователя
```javascript
// Выполните в консоли:
showUserInfo();
```

**Решение 2**: Восстановите из localStorage
```javascript
// Выполните в консоли:
currentUser = JSON.parse(localStorage.getItem('currentUser'));
showUserInfo();
```

**Решение 3**: Переавторизуйтесь
```javascript
// Выполните в консоли:
localStorage.removeItem('authToken');
localStorage.removeItem('currentUser');
location.reload();
```

---

## 📞 Контакты для поддержки

Если ничего не помогло, отправьте:
1. Скриншоты из раздела "Сбор информации для отчета"
2. Описание шагов, которые вы предприняли
3. Точное описание проблемы

---

## ✅ Чеклист диагностики

- [ ] Открыл DevTools (F12)
- [ ] Очистил консоль
- [ ] Воспроизвел проблему
- [ ] Проверил логи в Console
- [ ] Проверил запросы в Network
- [ ] Проверил LocalStorage
- [ ] Попробовал быстрые исправления
- [ ] Собрал информацию для отчета
- [ ] Проверил логи сервера (если есть доступ)

---

**Дата создания**: 2025-01-02  
**Версия**: 1.0
