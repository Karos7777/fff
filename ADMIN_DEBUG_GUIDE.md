# 🔍 ИНСТРУКЦИЯ ПО ОТЛАДКЕ АДМИНКИ

## ✅ Что сделано

### 1. Добавлены логи в `admin.js`
```js
console.log('🚀 ADMIN.JS ЗАГРУЖЕН!');  // Самая первая строка

document.addEventListener('DOMContentLoaded', function() {
    console.log('📋 [ADMIN] DOMContentLoaded - инициализация админки');
    initializeAdminPanel();
});

async function handleProductSubmit(e) {
    console.log('📝 [ADMIN FORM] Форма отправлена!');
    // ...
    console.log('🔍 [ADMIN FORM] Найдены чекбоксы:', { ... });
    console.log('📦 [ADMIN FORM] Чекбоксы из DOM:', { ... });
    console.log('📤 [ADMIN FORM] Отправка:', { ... });
}
```

### 2. Подключение в `admin.html`
```html
<script src="admin.js?v=2.0.0"></script>  <!-- Строка 247 -->
```

### 3. Статика в `server.js`
```js
app.use(express.static('public'));  // Должно быть!
```

---

## 🚀 КАК ПРОВЕРИТЬ

### Шаг 1: Открой админку
```
http://localhost:8080/admin.html
```

### Шаг 2: Открой консоль браузера
- Нажми **F12**
- Перейди на вкладку **Console**

### Шаг 3: Обнови страницу
- Нажми **Ctrl + F5** (жёсткая перезагрузка)

### Шаг 4: Проверь логи

**ДОЛЖНЫ ПОЯВИТЬСЯ:**
```
🚀 ADMIN.JS ЗАГРУЖЕН!
📋 [ADMIN] DOMContentLoaded - инициализация админки
```

**ЕСЛИ НЕТ** → `admin.js` не загружается!

---

## 🔧 ЕСЛИ admin.js НЕ ЗАГРУЖАЕТСЯ

### Проблема 1: 404 Not Found

**Проверь в консоли:**
```
GET http://localhost:8080/admin.js 404 (Not Found)
```

**Решение:**
1. Убедись что файл `public/admin.js` существует
2. Проверь `server.js`:
```js
app.use(express.static('public'));  // ДОЛЖНО БЫТЬ!
```

### Проблема 2: Кэш браузера

**Решение:**
1. Нажми **Ctrl + Shift + Delete**
2. Очисти кэш
3. Или открой в режиме инкогнито

### Проблема 3: Неправильный путь

**В `admin.html` должно быть:**
```html
<script src="admin.js?v=2.0.0"></script>  ✅
<!-- НЕ /public/admin.js -->
<!-- НЕ ./admin.js -->
```

---

## 📝 ПРОВЕРКА СОЗДАНИЯ ТОВАРА

### Шаг 1: Нажми "Добавить товар"

**ДОЛЖНЫ ПОЯВИТЬСЯ ЛОГИ:**
```
(Пока ничего - модальное окно открылось)
```

### Шаг 2: Заполни форму
- Название: "Тестовый товар"
- Цена TON: 1
- ✅ Отметь "Активный товар"

### Шаг 3: Нажми "Сохранить"

**ДОЛЖНЫ ПОЯВИТЬСЯ ЛОГИ:**
```
📝 [ADMIN FORM] Форма отправлена!
🔍 [ADMIN FORM] Найдены чекбоксы: {
  infiniteStockCheckbox: true,
  isActiveCheckbox: true
}
📦 [ADMIN FORM] Чекбоксы из DOM: {
  infiniteStockChecked: false,
  isActiveChecked: true  ← CHECKED!
}
📤 [ADMIN FORM] Отправка: {
  infinite_stock: 'off',
  is_active: 'on'  ← ОТПРАВЛЕНО!
}
```

### Шаг 4: Проверь логи сервера

**В терминале должно быть:**
```
➕ [ADMIN] Создание нового товара
📦 [ADMIN] Данные (raw): {
  name: 'Тестовый товар',
  price_ton: 1,
  is_active: 'on'  ← ПОЛУЧЕНО!
}
✅ [ADMIN] Обработано: {
  isActiveBool: true,  ← ПРЕОБРАЗОВАНО!
  raw_active: 'on'
}
✅ [ADMIN] Товар создан: {
  id: 17,
  name: 'Тестовый товар',
  is_active: true  ✅
}
```

---

## ❌ ЕСЛИ ЛОГОВ НЕТ

### Проблема: Форма не отправляется

**Проверь:**
1. ID формы в HTML:
```html
<form id="productForm">  <!-- Должно быть productForm -->
```

2. Обработчик в `admin.js`:
```js
const productForm = document.getElementById('productForm');
if (productForm) {
    productForm.addEventListener('submit', handleProductSubmit);
}
```

3. Проверь в консоли вручную:
```js
document.getElementById('productForm')
// Должно вернуть: <form id="productForm">...</form>
```

**ЕСЛИ `null`** → ID формы неправильный!

---

## ✅ ОЖИДАЕМЫЙ РЕЗУЛЬТАТ

### Консоль браузера:
```
🚀 ADMIN.JS ЗАГРУЖЕН!
📋 [ADMIN] DOMContentLoaded - инициализация админки
📝 [ADMIN FORM] Форма отправлена!
🔍 [ADMIN FORM] Найдены чекбоксы: { infiniteStockCheckbox: true, isActiveCheckbox: true }
📦 [ADMIN FORM] Чекбоксы из DOM: { infiniteStockChecked: false, isActiveChecked: true }
📤 [ADMIN FORM] Отправка: { infinite_stock: 'off', is_active: 'on' }
```

### Терминал сервера:
```
➕ [ADMIN] Создание нового товара
📦 [ADMIN] Данные (raw): { is_active: 'on' }
✅ [ADMIN] Обработано: { isActiveBool: true }
✅ [ADMIN] Товар создан: { id: 17, is_active: true }
```

### Результат:
- ✅ Товар создан
- ✅ `is_active: true`
- ✅ Товар появился в каталоге

---

## 🎯 ФИНАЛЬНАЯ ПРОВЕРКА

1. **Открой админку** → Должен появиться лог `🚀 ADMIN.JS ЗАГРУЖЕН!`
2. **Нажми "Добавить товар"** → Модальное окно открылось
3. **Заполни форму** → Отметь "Активный товар" ✅
4. **Нажми "Сохранить"** → Должны появиться логи формы
5. **Проверь терминал** → Должны появиться логи сервера
6. **Проверь каталог** → Товар должен появиться

**ЕСЛИ ВСЁ РАБОТАЕТ** → Деплой! 🚀

```bash
git add .
git commit -m "Add debug logs for admin.js and fix checkbox handling"
git push
```

**ЕСЛИ НЕ РАБОТАЕТ** → Покажи логи из консоли браузера!
